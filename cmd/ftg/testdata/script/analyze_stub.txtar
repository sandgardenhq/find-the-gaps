# analyze subcommand accepts --repo flag and scans an empty directory.
# --docs is required, so the run fails after scanning; the scan summary is
# still printed before the docs path runs.
mkdir repo
! exec ftg analyze --repo repo --cache-dir $WORK/cache --docs https://docs.example.invalid
stdout 'scanned 1 files'

-- repo/main.go --
package main
