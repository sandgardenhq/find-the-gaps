# analyze accepts LLM tier flags without error (no --docs-url, so no LLM call).
mkdir repo
exec ftg analyze --repo repo --cache-dir $WORK/cache --llm-small ollama/llama3 --llm-large anthropic/claude-haiku-4-5
stdout 'scanned 0 files'
