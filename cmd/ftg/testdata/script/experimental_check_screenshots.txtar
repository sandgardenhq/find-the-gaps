# analyze accepts --experimental-check-screenshots without error. --docs-url
# is required, so the run fails after scanning; the scan summary still prints
# first and the flag just needs to parse cleanly.
mkdir repo
! exec ftg analyze --repo repo --cache-dir $WORK/cache --docs-url https://docs.example.invalid --experimental-check-screenshots
stdout 'scanned 0 files'
